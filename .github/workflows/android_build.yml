name: Android Build

on:
  push:
    branches:
      - main
      - dev

jobs:
  build_android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x app/platform/android/app/gradlew

      - name: Build Android Release APK
        run: ./app/platform/android/app/gradlew assembleRelease

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: android-release-apk
          path: app/app/platform/android/app/build/outputs/apk/release/*.apk